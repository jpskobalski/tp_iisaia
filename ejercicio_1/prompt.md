Actúa como un Ingeniero de Software Senior experto en desarrollo Frontend y herramientas de IA. Necesito generar un proyecto para la materia Ingeniería de Software asistida por IA de la FIUBA.

Objetivo: Crear una "Interfaz de Usuario Frustrante" (User Hostile Design) que sea funcional pero deliberadamente molesta para el usuario.

Restricciones Técnicas:

Genera todo el código en un único archivo index.html.

Incluye el CSS dentro de etiquetas <style> y el JavaScript dentro de etiquetas <script>.

Usa HTML semántico (etiquetas main, section, header, footer).

En el CSS, define variables en :root para la paleta de colores (usa colores de alto contraste y visualmente agresivos).

Usa el Box Model de forma creativa para mover elementos y frustrar clics.

Funcionalidades de "Maldad" requeridas:

Botón Huidizo: Un botón de "Enviar" que, al detectar el evento mouseover, calcule una posición aleatoria y se mueva usando absolute positioning o margins, impidiendo que el usuario lo presione.

Formulario con Reset: Un campo de entrada de texto que, si el usuario deja de escribir por más de 2 segundos (usando un setTimeout), borre automáticamente todo el contenido.

Cursor Engañoso: Usa CSS para cambiar el cursor a un icono de "espera" (loading) permanentemente, aunque la página ya haya cargado.

Validación Infinita: Un checkbox de "No soy un robot" que, al ser marcado, desmarque automáticamente otros dos checkboxes obligatorios.

Output esperado: Solo el código del archivo index.html completo y funcional. No incluyas explicaciones externas, ya que el código debe hablar por sí mismo.

Haslo todo en el ejricio 1. Te adjunto la transcripcion de la clase por contexto